spring:
  cloud:
    kubernetes:
      config:
        enable-api: false
        paths: /etc/config/application-${spring.profiles.active}.yaml
      secrets:
        enable-api: false
        paths: /etc/secret
